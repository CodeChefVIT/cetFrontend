import React from "react";
import {
  // Button,
  // Card,
  // CardContent,
  // Dialog,
  // DialogActions,
  // DialogContent,
  // Grid,
  // Hidden,
  makeStyles,
  // TextField,
  // Typography,
} from "@material-ui/core";
import Background from "../../../hoc/Background/Background";

const useStyles = makeStyles((theme) => ({}));

const ClubProfile = () => {
  const classes = useStyles();
  return (
    <Background>
      <div>Club Profile</div>
    </Background>
  );
};

export default ClubProfile;
